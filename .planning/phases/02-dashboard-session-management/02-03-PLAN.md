---
phase: 02-dashboard-session-management
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/page.tsx
  - src/app/history/page.tsx
  - src/components/session/MobileNotice.tsx
  - src/app/session/[id]/page.tsx
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-06
  - INTERVIEW-05

must_haves:
  truths:
    - "Authenticated users visiting / are redirected to /dashboard immediately"
    - "/ renders marketing content for unauthenticated visitors (no auth-conditional sections)"
    - "/history redirects to /dashboard"
    - "Mobile visitors to /session/[id] see an amber notice banner above the canvas"
    - "Canvas shows only one set of undo/redo/delete controls (VendorToolbar only, no tldraw duplicates)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Marketing page with auth redirect for logged-in users"
      contains: "router.replace(\"/dashboard\")"
    - path: "src/app/history/page.tsx"
      provides: "Redirect to /dashboard"
      contains: "redirect(\"/dashboard\")"
    - path: "src/components/session/MobileNotice.tsx"
      provides: "Mobile canvas notice banner"
      exports: ["MobileNotice"]
    - path: "src/app/session/[id]/page.tsx"
      provides: "Session page with mobile notice rendered"
      contains: "MobileNotice"
  key_links:
    - from: "src/app/page.tsx"
      to: "/dashboard"
      via: "router.replace in useEffect"
      pattern: "router.replace.*dashboard"
    - from: "src/app/session/[id]/page.tsx"
      to: "src/components/session/MobileNotice.tsx"
      via: "import and render"
      pattern: "MobileNotice"
---

<objective>
Wire the auth redirect on the marketing page, consolidate /history into /dashboard, add the mobile canvas notice, and verify canvas controls are duplicate-free.

Purpose: Completes ONBOARD-01 (authenticated users land on dashboard), ONBOARD-06 (mobile responsiveness for session page via notice), and INTERVIEW-05 (single set of canvas controls). These are routing and UX cleanup tasks that can run in parallel with Plan 02.
Output: Modified src/app/page.tsx, replaced src/app/history/page.tsx, new src/components/session/MobileNotice.tsx, modified src/app/session/[id]/page.tsx.
</objective>

<execution_context>
@E:/Projects/archarena/.claude/get-shit-done/workflows/execute-plan.md
@E:/Projects/archarena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dashboard-session-management/02-CONTEXT.md
@.planning/phases/02-dashboard-session-management/02-PATTERNS.md
@.planning/phases/02-dashboard-session-management/02-UI-SPEC.md

Key decisions:
- D-01: Authenticated users redirected from / to /dashboard via client-side useEffect (no conditional rendering)
- D-03: /history replaced with a Next.js server redirect to /dashboard
- D-09: Mobile canvas page shows "use desktop" notice (CSS md:hidden — no JS media query)
- D-10: tldraw built-in toolbar already suppressed via Toolbar: null. Verify UndoRedoGroup is also gone. Only change code if a duplicate control is visible.
</context>

<interfaces>
<!-- Key signatures from files being modified -->

From src/app/page.tsx (existing, to be modified):
- "use client" component
- Already imports: `useSession` from next-auth/react, `useRouter` from next/navigation
- `status` variable from `const { data: session, status } = useSession()` already exists
- `router` variable from `const router = useRouter()` already exists
- Target: add useEffect after existing hooks that calls `router.replace("/dashboard")` when `status === "authenticated"`, and add `if (status === "authenticated") return null` guard

From src/components/canvas/InterviewCanvas.tsx (verify-only):
- TLDRAW_COMPONENT_OVERRIDES already sets `Toolbar: null` — this suppresses the entire toolbar including UndoRedoGroup
- No code change should be needed for INTERVIEW-05
- Verify by reading the file; only modify if an explicit `UndoRedoGroup` render is found outside this object

From src/app/session/[id]/page.tsx (to be modified):
- Dynamic import of InterviewCanvas
- Renders the main session layout
- MobileNotice must be placed at the top of the returned JSX, inside the outermost div, before the canvas area
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Auth redirect on marketing page, /history redirect, and MobileNotice component</name>
  <read_first>
    - src/app/page.tsx (lines 1-50 to find where hooks are declared; lines 770-790 to find exact insertion point after existing hooks)
    - src/app/history/page.tsx (understand current content before replacing)
    - .planning/phases/02-dashboard-session-management/02-PATTERNS.md (page.tsx redirect pattern, history redirect pattern, MobileNotice pattern)
    - .planning/phases/02-dashboard-session-management/02-UI-SPEC.md (MobileNotice component spec)
  </read_first>
  <files>src/app/page.tsx, src/app/history/page.tsx, src/components/session/MobileNotice.tsx</files>
  <action>
**Change 1: src/app/page.tsx — add auth redirect**

Read the current file to find the `Home()` component function body. Locate the lines where `status` and `router` are already declared (from `useSession()` and `useRouter()` calls).

Add two things inside the `Home()` function, immediately after the existing hook declarations:

```typescript
// D-01: redirect authenticated users to /dashboard
// Marketing page stays pure for logged-out visitors — no conditional rendering
useEffect(() => {
  if (status === "authenticated") {
    router.replace("/dashboard")
  }
}, [status, router])

if (status === "authenticated") return null
```

The `useEffect` import is already present in page.tsx (it uses hooks). Add it to the existing import if not already there.

Do NOT add any conditional sections for logged-in vs logged-out content. The page returns null for authenticated users and lets the redirect handle navigation.

**Change 2: src/app/history/page.tsx — replace with server redirect**

Replace the entire file content with:

```typescript
import { redirect } from "next/navigation"

// D-03: /history consolidated into /dashboard
// Existing bookmarks to /history continue to work via this redirect.
export default function HistoryPage() {
  redirect("/dashboard")
}
```

This is a server component (no "use client"). Uses Next.js `redirect()` which is a permanent redirect.

**Change 3: src/components/session/MobileNotice.tsx — create new component**

Create the directory src/components/session/ if it doesn't exist, then create:

```typescript
"use client"

import { Monitor } from "lucide-react"

// D-09: Show a notice on mobile viewports instead of attempting touch canvas support.
// Hidden at md breakpoint and above via Tailwind md:hidden.
export function MobileNotice() {
  return (
    <div className="md:hidden bg-amber-900/30 border border-amber-700/50 rounded-lg p-4 mx-4 mt-4 flex items-center gap-3">
      <Monitor className="h-5 w-5 text-amber-300 shrink-0" />
      <p className="text-sm text-amber-200">
        This experience works best on desktop. Switch to a larger screen for the full canvas.
      </p>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /e/Projects/archarena && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "router.replace.*dashboard" src/app/page.tsx` returns a match
    - `grep 'if (status === "authenticated") return null' src/app/page.tsx` returns a match
    - `grep "conditional\|loggedIn\|isAuthenticated" src/app/page.tsx` returns NO match (no conditional rendering based on auth — redirect only)
    - `grep 'redirect("/dashboard")' src/app/history/page.tsx` returns a match
    - `grep '"use client"' src/app/history/page.tsx` returns NO match (must be server component)
    - `grep "export function MobileNotice" src/components/session/MobileNotice.tsx` returns a match
    - `grep "md:hidden" src/components/session/MobileNotice.tsx` returns a match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Auth redirect active on marketing page. /history redirects to /dashboard. MobileNotice component created with correct CSS visibility class.</done>
</task>

<task type="auto">
  <name>Task 2: Add MobileNotice to session page and verify canvas controls</name>
  <read_first>
    - src/app/session/[id]/page.tsx (full file — find the return JSX structure to locate correct insertion point for MobileNotice)
    - src/components/canvas/InterviewCanvas.tsx lines 25-40 (verify TLDRAW_COMPONENT_OVERRIDES — confirm Toolbar: null is set and no UndoRedoGroup renders outside this object)
    - src/components/canvas/toolbar/VendorToolbar.tsx (confirm this is the only toolbar we keep — verify it contains undo, redo, delete)
  </read_first>
  <files>src/app/session/[id]/page.tsx</files>
  <action>
**Step 1: Verify canvas controls (read-only check)**

Read src/components/canvas/InterviewCanvas.tsx and confirm:
- `TLDRAW_COMPONENT_OVERRIDES` has `Toolbar: null`
- No `UndoRedoGroup` component is imported or rendered anywhere in the file
- No `ActionsMenu` is rendered (it's also null in overrides)

Read src/components/canvas/toolbar/VendorToolbar.tsx and confirm it contains undo, redo, and delete controls.

If both are confirmed: INTERVIEW-05 is already satisfied. No code change needed to InterviewCanvas.tsx.

If `UndoRedoGroup` IS found rendering outside `TLDRAW_COMPONENT_OVERRIDES`: add `UndoRedoGroup: null` to the overrides object. Only make this change if the component is actually found rendering.

**Step 2: Add MobileNotice to session page**

Read src/app/session/[id]/page.tsx to find the outermost return statement.

Add the MobileNotice import at the top of the file:
```typescript
import { MobileNotice } from "@/components/session/MobileNotice"
```

In the return JSX, place `<MobileNotice />` as the first element inside the outermost div (before the canvas/chat layout). Example position:

```tsx
return (
  <div className="...existing outermost div classes...">
    <MobileNotice />
    {/* ... existing session content ... */}
  </div>
)
```

Do not change any other part of the session page. The MobileNotice uses `md:hidden` so it is invisible on desktop — no layout disruption.
  </action>
  <verify>
    <automated>cd /e/Projects/archarena && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MobileNotice" src/app/session/[id]/page.tsx` returns at least 2 matches (import + usage)
    - `grep "import.*MobileNotice" src/app/session/[id]/page.tsx` returns a match
    - `grep "UndoRedoGroup" src/components/canvas/InterviewCanvas.tsx` returns NO match (or if it exists, it is set to null in TLDRAW_COMPONENT_OVERRIDES)
    - `grep "Toolbar: null" src/components/canvas/InterviewCanvas.tsx` returns a match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MobileNotice renders on session page (visible only on mobile via md:hidden). Canvas controls verified to have no duplicates — one set of undo/redo/delete from VendorToolbar only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → / | Public marketing route; authenticated users must not see auth-state-conditional data |
| browser → /history | Legacy route; server-side redirect, no data exposed |
| browser → /session/[id] | Protected session route; MobileNotice is purely presentational |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | src/app/page.tsx auth redirect | mitigate | `return null` after redirect prevents any authenticated user data from rendering on the marketing page; marketing page remains stateless for logged-out visitors |
| T-02-09 | Spoofing | /history redirect | accept | Server-side `redirect()` is non-interactive; no auth data is exposed; the destination /dashboard enforces its own auth guard |
| T-02-10 | Information Disclosure | MobileNotice component | accept | Component is purely presentational text with no user data; no security surface |
</threat_model>

<verification>
1. Visit / as authenticated user → immediately redirected to /dashboard (no flash of marketing content)
2. Visit / as unauthenticated user → marketing page renders normally with prompt grid
3. Visit /history → redirected to /dashboard
4. Visit /session/[id] on a mobile-width browser (375px) → amber notice banner visible above canvas
5. Visit /session/[id] on desktop → no notice banner visible (md:hidden working)
6. Open canvas on desktop → only VendorToolbar undo/redo/delete visible, no tldraw duplicate toolbar
7. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Authenticated users redirected from / to /dashboard
- / renders unchanged for unauthenticated visitors
- /history redirects to /dashboard
- MobileNotice shows on mobile, hidden on md+ screens
- Zero duplicate undo/redo/delete controls on canvas
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-session-management/02-03-SUMMARY.md`
</output>
